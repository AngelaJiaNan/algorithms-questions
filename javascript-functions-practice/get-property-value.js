/* exported getPropertyValue */
function getPropertyValue(object, key) {
  return object[key];
}

// this value is a string so [];
// const obj = {
//   'name': 'Angela'
// };

// obj.name

// {}, 'name'

// obj['name']
